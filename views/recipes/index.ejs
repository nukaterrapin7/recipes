<%- include('../partials/header') %>

<table id="list">
  <thead>
    <tr>
      <th>Recipe Name</th>
      <!-- <th>Ingredients</th>
      <th>Directions</th> -->
    </tr>
  </thead>
  <tbody>
    <% recipes.forEach(function(r) { %>
      <tr>
        <td><%= r.name %></td>
        <!-- <td><%= r.ingredients %></td>
        <td><%= r.directions %></td> -->
        <td><a href="/recipes/<%= r.id %>"> SHOW RECIPE</a></td>
      </tr>
      <% }); %>
    </tbody>
  </table>
  
<%- include('../partials/footer') %>